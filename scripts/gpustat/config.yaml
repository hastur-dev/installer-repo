# gpustat - GPU Monitoring Tool
name: gpustat
description: Simple GPU monitoring utility
category: monitoring

# Capabilities
choose-file: false
has-repl: false
is-package-manager: false

# Commands
command: gpustat
watch-command: "gpustat -i"
