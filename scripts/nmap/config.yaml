# nmap - Network Scanner
name: nmap
description: Network exploration and security auditing
category: networking

# Capabilities
choose-file: false
has-repl: false
is-package-manager: false

# Commands
command: nmap
scan-command: "nmap {target}"
port-scan-command: "nmap -p {ports} {target}"
